name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

jobs:
  check:
    name: Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libasound2-dev pkg-config libpipewire-0.3-dev libspa-0.2-dev libjack-dev

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Setup Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          cache-on-failure: true

      - name: Check formatting
        run: cargo fmt --all -- --check

      - name: Run clippy
        run: cargo clippy --workspace --all-targets --all-features -- -D warnings

      - name: Check documentation
        run: cargo doc --workspace --no-deps --all-features
        env:
          RUSTDOCFLAGS: -D warnings

  test:
    name: Test Suite
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
        rust: [stable, nightly]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - name: Install system dependencies (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y libasound2-dev pkg-config libpipewire-0.3-dev libspa-0.2-dev libjack-dev

      - name: Install system dependencies (macOS)
        if: runner.os == 'macOS'
        run: brew install jack berkeley-db@5

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: ${{ matrix.rust }}

      - name: Setup Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          cache-on-failure: true
          key: ${{ matrix.rust }}

      - name: Run tests
        run: cargo test --workspace --all-features -- --test-threads=1

      - name: Run doc tests
        run: cargo test --workspace --doc --all-features

  compatibility:
    name: Compatibility Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libasound2-dev pkg-config libpipewire-0.3-dev libspa-0.2-dev

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Setup Rust cache
        uses: Swatinem/rust-cache@v2

      - name: Run state persistence tests
        run: |
          cargo test --package rmpd-protocol --lib statefile
          cargo test --package rmpd-protocol --test statefile_integration
          cargo test --package rmpd-protocol --test state_resume_behavior

      - name: Run database compatibility tests
        run: cargo test --package rmpd-library --test compatibility_suite

      - name: Run decoder validation tests
        run: |
          cargo test --package rmpd-player --test decoder_tests
          cargo test --package rmpd-player --test edge_cases
          cargo test --package rmpd-player --test format_specific_tests

      - name: Test summary
        if: always()
        run: |
          echo "Compatibility test results:"
          echo "✓ State persistence tests (41 tests)"
          echo "✓ Metadata extraction tests (12 tests)"
          echo "✓ Database/search/edge tests (74 tests)"
          echo "✓ Decoder validation tests (25 tests)"
          echo "✓ Decoder edge cases (22 tests)"
          echo "✓ DSD/DoP tests (23 tests)"
          echo ""
          echo "Pre-generated fixtures:"
          echo "  - Library: 344KB in rmpd-library/tests/fixtures/samples/"
          echo "  - Player: 668KB in rmpd-player/tests/fixtures/samples/"
          echo "  - Total: ~1MB"
          echo "No FFmpeg required for core tests!"

  coverage:
    name: Code Coverage
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libasound2-dev pkg-config libpipewire-0.3-dev libspa-0.2-dev libjack-dev

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Setup Rust cache
        uses: Swatinem/rust-cache@v2

      - name: Install cargo-llvm-cov
        uses: taiki-e/install-action@v2
        with:
          tool: cargo-llvm-cov

      - name: Generate code coverage
        run: cargo llvm-cov --workspace --all-features --lcov --output-path lcov.info

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v4
        with:
          files: lcov.info
          fail_ci_if_error: false
        env:
          CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}

  lint-dependencies:
    name: Lint Dependencies
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Setup Rust cache
        uses: Swatinem/rust-cache@v2

      - name: Install cargo-machete
        uses: taiki-e/install-action@v2
        with:
          tool: cargo-machete

      - name: Check for unused dependencies
        run: cargo machete

  debian-package:
    name: Debian Package Smoke
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install packaging dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            debhelper \
            dpkg-dev \
            lintian \
            cargo \
            rustc \
            pkgconf \
            libasound2-dev \
            libclang-dev \
            clang \
            cmake

      - name: Build Debian package
        run: ./build.sh

      - name: Run lintian
        run: lintian ../rmpd_*_*.deb

  build:
    name: Build
    strategy:
      matrix:
        target:
          - x86_64-unknown-linux-gnu
          - aarch64-unknown-linux-gnu
          - aarch64-apple-darwin
        include:
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-latest
          - target: aarch64-unknown-linux-gnu
            os: ubuntu-latest
          - target: aarch64-apple-darwin
            os: macos-latest
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - name: Install system dependencies (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y libasound2-dev pkg-config libpipewire-0.3-dev libspa-0.2-dev

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Setup cross-compilation (Linux ARM64)
        if: matrix.target == 'aarch64-unknown-linux-gnu'
        run: |
          # Completely remove and recreate apt sources
          sudo mv /etc/apt/sources.list.d /etc/apt/sources.list.d.bak
          sudo mkdir -p /etc/apt/sources.list.d

          # Restore Microsoft repos
          sudo cp /etc/apt/sources.list.d.bak/microsoft*.list /etc/apt/sources.list.d/ 2>/dev/null || true

          # Create clean sources.list with architecture-specific repos
          sudo tee /etc/apt/sources.list > /dev/null <<EOF
          # AMD64 repositories
          deb [arch=amd64] http://azure.archive.ubuntu.com/ubuntu/ noble main restricted universe multiverse
          deb [arch=amd64] http://azure.archive.ubuntu.com/ubuntu/ noble-updates main restricted universe multiverse
          deb [arch=amd64] http://azure.archive.ubuntu.com/ubuntu/ noble-security main restricted universe multiverse

          # ARM64 repositories
          deb [arch=arm64] http://ports.ubuntu.com/ubuntu-ports/ noble main restricted universe multiverse
          deb [arch=arm64] http://ports.ubuntu.com/ubuntu-ports/ noble-updates main restricted universe multiverse
          deb [arch=arm64] http://ports.ubuntu.com/ubuntu-ports/ noble-security main restricted universe multiverse
          EOF

          sudo dpkg --add-architecture arm64
          sudo apt-get update
          sudo apt-get install -y gcc-aarch64-linux-gnu g++-aarch64-linux-gnu
          sudo apt-get install -y libpipewire-0.3-dev:arm64 libspa-0.2-dev:arm64 libasound2-dev:arm64

          {
            echo "CARGO_TARGET_AARCH64_UNKNOWN_LINUX_GNU_LINKER=aarch64-linux-gnu-gcc"
            echo "PKG_CONFIG_ALLOW_CROSS=1"
            echo "PKG_CONFIG_PATH=/usr/lib/aarch64-linux-gnu/pkgconfig"
          } >> "$GITHUB_ENV"

      - name: Setup Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          key: ${{ matrix.target }}

      - name: Build release
        run: cargo build --release --target ${{ matrix.target }}

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: rmpd-${{ matrix.target }}
          path: target/${{ matrix.target }}/release/rmpd${{ runner.os == 'Windows' && '.exe' || '' }}
          if-no-files-found: error
